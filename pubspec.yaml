name: arcgis_maps_toolkit
description: A toolkit of widgets for use with the ArcGIS Maps SDK for Flutter.
version: 0.0.1
homepage: https://developers.arcgis.com/flutter/
topics:
  - map
  - location

publish_to: none

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  flutter:
    sdk: flutter
  arcgis_maps: ^200.6.0+4426

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
